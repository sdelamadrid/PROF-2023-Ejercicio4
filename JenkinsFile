pipeline {
    agent any

    stages {
        stage('Erase DB') {
            steps {
                sh 'sudo sqlite3 Employees.db < errasedb.sql'
            }
        }
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
