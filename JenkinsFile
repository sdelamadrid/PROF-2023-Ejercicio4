pipeline {
    agent any

    stages {
        stage('Erase DB') {
            steps {
                sh 'sqlite3 Employees.db < erasedb.sql'
            }
        }
        stage('New Schema') {
            steps {
                sh 'sqlite3 Employees.db < sqlite.sql'
            }
        }
    }
}
